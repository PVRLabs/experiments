# Deterministic, guest-local configuration for the extracted-layout experiment.
# The normal application profile in application.properties remains unchanged.
spring.h2.console.enabled=false
spring.datasource.url=jdbc:h2:file:./data/starsdb
app.github.repos=PVRLabs/statlite
app.github.api-base-url=http://127.0.0.1:9
app.github.token=
